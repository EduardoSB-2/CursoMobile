class PhotoModel {
  //atributos
  final String location;
  final String dateTime;
  final String photoPath;

  // construtor
  PhotoModel({
    required this.location, required this.dateTime, required this.photoPath
  });
  
}